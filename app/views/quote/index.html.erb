<%= render "quotes/#{@type.underscore}_form" %>

<%= render "quote/hero" %>

<div class="container">
  <div class="pull_up quotes">
    <p>
      Please choose an insurance coverage type  to quote and answer the following questions.<span class="db">  We will get back to you promptly. We will never sell your information. It will always remain confidential and secure.</span> 
    </p>
    <div class="blue-header">
      <div class="quote-type-wrapper">

        <div class="dropdown">
          <div class="dropdown-container">
            <p class="dropdown-button"><%= @type.titleize.split(" ")[0] %> Quote  </p>
            <ul class="dropdown-menu dropdown-select">
              <% quote_types.each do |quote_type| %>
                <li> <a href="/quote?type=<%= quote_type[:type] %>">
                  <%= quote_type[:name] %>
                  </a></li>
              <% end %>
            </ul>
          </div>
        </div>

      </div>

    </div>

    <% if @quote.persisted? %>
      Thank you! We will get in touch soon.
    <% else %>
<div class="forms quote-page <%= @type %> animated  fadeIn">
      <%= yield :quote_form %>
    <% end %>
    </div>
  </div>
</div>

<script type="text/javascript">
$(document).ready(function() {

  // $(".dropdown-select").change(function(e) {
  //   window.location.href = "/quote?type=" + $('#quoteType').val();
  // })

  $(".dropdown-button").click(function() {
    var $button, $menu;
    $button = $(this);
    $menu = $button.siblings(".dropdown-menu");
    $menu.toggleClass("show-menu");
    // $menu.children("li").click(function() {
      // $menu.removeClass("show-menu");
      // $button.html($(this).html());
    // });
  });

});



</script>